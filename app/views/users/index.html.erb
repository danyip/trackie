<h2>User Index</h2>

<ul>

  <% @users.each do |user|%>
    <li>
      <%= link_to (image_tag user.profile_pic, class: "profile-pic-small"), user_path(user.id) %>
      <%= link_to "#{user.first_name} #{user.last_name}", user_path(user.id) %>
    </li>
  <% end %>

</ul>
